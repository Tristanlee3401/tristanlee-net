<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="initial-scale=1.0, width=device-width">
    <link href="{{ '/styles-2.css' | url }}" rel="stylesheet">
    <title>{{ title }}</title>
</head>
<body class="home">
    {{ content }}
    <script src="{{ '/script.js' | url }}"></script>
</body>
</html>
